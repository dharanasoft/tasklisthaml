%h1 Listing Completed Tasks
%ul.tasks
  - @completed.each do |task|
    %li.task
      =task.title
      =link_to 'delete', task_path(task), :method=>'delete',:confirm=>'Are you sure you want to delete?'

= link_to 'Back to pending tasks',bucket_tasks_path(@bucket)
